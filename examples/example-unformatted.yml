tasks: # tasks section
  build:
    cmds:
      - echo "Building..."
  # testsuete
  test_suite:
    cmds:
      - echo "Testing..."
  docker_stuff:

    # above cmds section
    cmds:
      - echo "Docker stuff"
env:
  NODE_ENV: development
includes:
  - ./common.yml
vars:
  PROJECT: myproject
  lowercase_var: should be uppercase
  another_var: value
tasks_with_templates:
  # greet task
  greet:
    cmds:
      - echo "Hello " # Template variable
      - |

        echo "Project: "
version: '3'
# under version section
